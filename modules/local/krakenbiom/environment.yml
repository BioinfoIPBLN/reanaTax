---
# yaml-language-server: $schema=https://raw.githubusercontent.com/nf-core/modules/master/modules/environment-schema.json
# bin/biom_export.py needs the biom API, which kraken-biom already depends on.
channels:
  - conda-forge
  - bioconda
dependencies:
  - bioconda::kraken-biom=1.2.0
