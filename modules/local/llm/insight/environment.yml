---
# yaml-language-server: $schema=https://raw.githubusercontent.com/nf-core/modules/master/modules/environment-schema.json
# bin/llm_insight.py and bin/llm_common.py use only the Python standard library,
# so the environment is a bare interpreter.
channels:
  - conda-forge
  - bioconda
dependencies:
  - conda-forge::python=3.12
